import { Meta } from '@storybook/react';
import { HtmlEditor } from './html-editor';

export default {
  title: 'Ultra UI/HtmlEditor',
  component: HtmlEditor,
  tags: ['Ultra UI', 'HtmlEditor']
} as Meta;

export const HtmlEditorDefault = {
  args: {}
};
